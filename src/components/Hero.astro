---
const terminalLines = [
  { prompt: true, text: 'sss agent deploy support-triage --env prod' },
  { cls: 'dim', text: 'resolving tools ......... crm, kb-search, escalate' },
  { cls: 'dim', text: 'wiring guardrails ....... pii-filter, policy-check' },
  { cls: 'ok', text: 'eval suite ............. 312/312 passed (p95 1.2s)' },
  { cls: 'ok', text: 'canary 5% .............. CSAT +18% vs control' },
  { cls: 'accent', text: '▸ promoted to 100% — live' },
];
---

<section class="hero" id="top">
  <canvas class="hero-field" id="hero-field" aria-hidden="true"></canvas>
  <div class="hero-vignette" aria-hidden="true"></div>

  <div class="container hero-inner">
    <div class="hero-copy">
      <p class="eyebrow">AI-native consultancy</p>
      <h1 class="hero-title">
        Frontier AI,<br />
        <span class="highlight">forged into production.</span>
      </h1>
      <p class="hero-lede">
        We design, build, and ship agentic systems, LLM products, and the platforms beneath
        them. Senior engineers, measurable results — in weeks, not quarters.
      </p>
      <div class="hero-actions">
        <a href="mailto:contact@siliconsteelsoftware.com" class="btn btn-primary">
          Start a conversation
        </a>
        <a href="#capabilities" class="btn btn-ghost">Explore capabilities</a>
      </div>
    </div>

    <div class="terminal" aria-hidden="true">
      <div class="terminal-bar">
        <span class="terminal-dot"></span>
        <span class="terminal-dot"></span>
        <span class="terminal-dot"></span>
        <span class="terminal-name">agent-run — prod</span>
      </div>
      <div class="terminal-body">
        {
          terminalLines.map(({ prompt, cls, text }, i) => (
            <p class={`terminal-line ${cls ?? ''}`} style={`--i: ${i}`}>
              {prompt && <span class="terminal-prompt">❯</span>}
              {text}
            </p>
          ))
        }
        <p class="terminal-line caret" style={`--i: ${terminalLines.length}`}>
          <span class="terminal-prompt">❯</span>
          <span class="terminal-caret"></span>
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    display: flex;
    align-items: center;
    min-height: 100svh;
    padding-top: var(--nav-height);
    overflow: hidden;
  }

  .hero-field {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }

  .hero-vignette {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 70% 60% at 28% 45%, rgba(255, 255, 255, 0.88), transparent 70%),
      linear-gradient(180deg, rgba(255, 255, 255, 0.6), transparent 30%, transparent 75%, var(--bg));
    pointer-events: none;
  }

  .hero-inner {
    position: relative;
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    align-items: center;
    gap: 4rem;
    padding-block: 6rem;
    width: 100%;
  }

  .hero-copy {
    display: grid;
    gap: 1.75rem;
    justify-items: start;
  }

  .hero-copy > * {
    animation: hero-rise 700ms var(--ease) both;
  }

  .hero-copy > :nth-child(2) {
    animation-delay: 90ms;
  }

  .hero-copy > :nth-child(3) {
    animation-delay: 180ms;
  }

  .hero-copy > :nth-child(4) {
    animation-delay: 270ms;
  }

  .hero-title {
    font-size: clamp(2.75rem, 6.5vw, 4.75rem);
    font-weight: 700;
  }

  .hero-lede {
    max-width: 34rem;
    font-size: clamp(1.0625rem, 1.8vw, 1.1875rem);
    color: var(--ink-soft);
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  /* Terminal — black screen on the white page */
  .terminal {
    font-family: var(--font-mono);
    font-size: 0.8125rem;
    background: var(--screen);
    border: 1px solid var(--ink);
    border-radius: 1rem;
    box-shadow: 6px 6px 0 0 var(--accent), 6px 6px 0 1px var(--ink);
    animation: hero-rise 800ms 350ms var(--ease) both;
  }

  .terminal-bar {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    border-bottom: 1px solid var(--screen-line);
  }

  .terminal-dot {
    width: 0.625rem;
    height: 0.625rem;
    border-radius: 50%;
    background: #ff5f57;
    opacity: 0.9;
  }

  .terminal-dot:nth-child(2) {
    background: #febc2e;
  }

  .terminal-dot:nth-child(3) {
    background: #28c840;
  }

  .terminal-name {
    margin-left: 0.5rem;
    color: var(--screen-muted);
    font-size: 0.75rem;
  }

  .terminal-body {
    padding: 1.25rem 1.25rem 1.5rem;
    display: grid;
    gap: 0.55rem;
  }

  .terminal-line {
    color: var(--screen-text);
    white-space: nowrap;
    overflow: hidden;
    opacity: 0;
    animation: line-in 300ms var(--ease) forwards;
    animation-delay: calc(1s + var(--i) * 0.55s);
  }

  .terminal-line.dim {
    color: var(--screen-muted);
  }

  .terminal-line.ok {
    color: #7ee787;
  }

  .terminal-line.accent {
    color: var(--accent);
    text-shadow: 0 0 18px var(--accent-glow);
  }

  .terminal-prompt {
    color: var(--accent);
    margin-right: 0.5rem;
  }

  .terminal-caret {
    display: inline-block;
    width: 0.55em;
    height: 1.05em;
    vertical-align: text-bottom;
    background: var(--accent);
    animation: caret-blink 1.1s steps(1) infinite;
  }

  @keyframes hero-rise {
    from {
      opacity: 0;
      transform: translateY(16px);
    }
    to {
      opacity: 1;
      transform: none;
    }
  }

  @keyframes line-in {
    from {
      opacity: 0;
      transform: translateY(4px);
    }
    to {
      opacity: 1;
      transform: none;
    }
  }

  @keyframes caret-blink {
    50% {
      opacity: 0;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .terminal-line,
    .hero-copy > *,
    .terminal {
      opacity: 1;
      animation: none;
    }
  }

  @media (max-width: 1024px) {
    .hero-inner {
      grid-template-columns: 1fr;
      gap: 3rem;
      padding-block: 5rem;
    }

    .terminal {
      max-width: 34rem;
    }
  }

  @media (max-width: 480px) {
    .terminal {
      font-size: 0.6875rem;
    }
  }
</style>

<script>
  // Ink particle field with occasional fluoro nodes, linked by proximity.
  const canvas = document.getElementById('hero-field') as HTMLCanvasElement | null;
  const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  if (canvas) {
    const ctx = canvas.getContext('2d')!;
    const dpr = Math.min(window.devicePixelRatio || 1, 2);
    let w = 0;
    let h = 0;
    let raf = 0;

    type P = { x: number; y: number; vx: number; vy: number; r: number; hot: boolean };
    let particles: P[] = [];

    const resize = () => {
      w = canvas.clientWidth;
      h = canvas.clientHeight;
      canvas.width = w * dpr;
      canvas.height = h * dpr;
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      const count = Math.min(110, Math.floor((w * h) / 16000));
      particles = Array.from({ length: count }, (_, i) => ({
        x: Math.random() * w,
        y: Math.random() * h,
        vx: (Math.random() - 0.5) * 0.35,
        vy: (Math.random() - 0.5) * 0.35,
        r: 1.2 + Math.random() * 1.6,
        hot: i % 9 === 0, // every ninth node is fluoro
      }));
    };

    const LINK = 130;

    const draw = () => {
      ctx.clearRect(0, 0, w, h);

      for (const p of particles) {
        p.x += p.vx;
        p.y += p.vy;
        if (p.x < -20) p.x = w + 20;
        if (p.x > w + 20) p.x = -20;
        if (p.y < -20) p.y = h + 20;
        if (p.y > h + 20) p.y = -20;
      }

      for (let i = 0; i < particles.length; i++) {
        const a = particles[i];
        for (let j = i + 1; j < particles.length; j++) {
          const b = particles[j];
          const dx = a.x - b.x;
          const dy = a.y - b.y;
          const d2 = dx * dx + dy * dy;
          if (d2 < LINK * LINK) {
            const alpha = (1 - Math.sqrt(d2) / LINK) * 0.16;
            ctx.strokeStyle = `rgba(10, 10, 10, ${alpha})`;
            ctx.lineWidth = 1;
            ctx.beginPath();
            ctx.moveTo(a.x, a.y);
            ctx.lineTo(b.x, b.y);
            ctx.stroke();
          }
        }
      }

      for (const p of particles) {
        ctx.fillStyle = p.hot ? 'rgba(173, 222, 0, 0.95)' : 'rgba(10, 10, 10, 0.55)';
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.hot ? p.r + 1 : p.r, 0, Math.PI * 2);
        ctx.fill();
      }
    };

    const loop = () => {
      draw();
      raf = requestAnimationFrame(loop);
    };

    resize();
    window.addEventListener('resize', resize);

    if (reduced) {
      draw(); // static frame
    } else {
      // Pause the loop when the hero is offscreen.
      const observer = new IntersectionObserver(([entry]) => {
        if (entry.isIntersecting) {
          if (!raf) raf = requestAnimationFrame(loop);
        } else {
          cancelAnimationFrame(raf);
          raf = 0;
        }
      });
      observer.observe(canvas);
    }
  }
</script>
